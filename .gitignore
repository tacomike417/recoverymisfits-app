# Generated by scripts/build_pages.py for local preview — never commit these.
# The live site is built fresh on every deploy by .github/workflows/deploy.yml.
/another-day-sober/
/sitemap.xml
/_site/

# local backups from tools/shrink-*.py — never commit these
/_audio-backup-*/
/assets/_art-backup-*/

# Written by scripts/fetch_tapes.py. On deploy it goes straight into the
# build folder; this line only catches a local run made without ADS_OUT_ROOT.
/data/tapes.js

# Written by scripts/build_pages.py from data/memes.json -- which meme runs
# on which day. Rebuilt on every deploy; a local run writes it here too.
/data/meme-days.json

recovery-misfits-first-five-coins/
Claude outputs/

recovery-misfits-first-five-coins/
Claude outputs/

recovery-misfits-first-five-coins/
Claude outputs/

recovery-misfits-first-five-coins/
Claude outputs/

# The drop box for scripts/add_memes.py. Memes come OUT of here into
# assets/memes/ when that runs; nothing in here is part of the site.
/assets/memes/new/
